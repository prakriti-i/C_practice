#include<stdio.h>
#include<conio.h>
int main()
{
	int a=10,b=5,c;
	c=a+b;
	printf("sum in hexadecimal is %X",c);
	printf("\nsum in hexadecimal is %x",c);
	printf("\nsum in decimal is %d",c);
	printf("\nsum in octal is %o",c);
	getch();
	return 0;
	
}
